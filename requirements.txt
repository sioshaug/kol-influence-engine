# Flexible ranges with upper bounds: lets the host resolve a mutually-compatible
# set for its Python version (avoids exact-pin install/ABI mismatches on the
# cloud), while the upper bounds guard against a future major release breaking it.
streamlit>=1.39,<2
pandas>=2.0,<3
numpy>=1.24,<3
networkx>=3.1,<4
requests>=2.31,<3
pyvis>=0.3.2,<0.4
